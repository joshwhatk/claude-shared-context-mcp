# =============================================================================
# MCP Shared Context Server - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment.
# For docker-compose, run: npm run docker:up

# -----------------------------------------------------------------------------
# Required: PostgreSQL connection string
# -----------------------------------------------------------------------------
# Local with docker-compose: postgresql://postgres:dev@localhost:5432/mcp_shared_context
# Railway: Auto-injected as DATABASE_URL
DATABASE_URL=postgresql://postgres:dev@localhost:5432/mcp_shared_context

# -----------------------------------------------------------------------------
# Required: Clerk authentication keys
# -----------------------------------------------------------------------------
# Get these from https://dashboard.clerk.com → Your app → API Keys
CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Frontend needs the publishable key exposed via Vite
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# -----------------------------------------------------------------------------
# Required: Admin bootstrap
# -----------------------------------------------------------------------------
# Email address to auto-provision as admin on first Clerk login
ADMIN_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# Optional: Server configuration
# -----------------------------------------------------------------------------
# Server port (default: 3000)
PORT=3000

# Environment (development/production)
NODE_ENV=development

# Logging level (info/debug)
LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Optional: Test database (for running tests)
# -----------------------------------------------------------------------------
# docker-compose exposes test DB on port 5433
TEST_DATABASE_URL=postgresql://postgres:dev@localhost:5433/mcp_shared_context_test
